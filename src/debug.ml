let log_cm = true   (* classmapper *)
let log_cp = true   (* classpath; bytecode lookup *)
let log_re = true   (* related to regular expressions *)

let log_dbg = true  (* category used only during debigging of small parts *)
